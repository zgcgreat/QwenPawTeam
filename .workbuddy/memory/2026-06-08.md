# 2026-06-08

- 使用 FlowForge Strict 模式启动“将 upstream QwenPaw 最新功能适配到多用户二开版本”的任务；已完成初步仓库调查：当前 main 分支相对 origin/main ahead 221，upstream/main 已 fetch 到 4c85867c；本地存在未提交改动 `.env`、`console/pnpm-lock.yaml`、`src/qwenpaw/app/routers/agents.py`、`src/qwenpaw/app/routers/auth.py`、`src/qwenpaw_plugins/multi_user/auth_extension.py`。识别多用户保护边界为 `src/qwenpaw_plugins/multi_user/`、`console/src/multi_user/` 及少量 upstream 接入点如 `src/qwenpaw/app/_app.py`、routers、workspace、auth 响应模型。形成原则：尽量不改 upstream 主体，优先通过插件、hook、monkey patch、前端请求扩展和少量接入点完成适配。
