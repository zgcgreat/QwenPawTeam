# 2026-06-11 QwenPawTeam 工作日志

## 系统性审核多用户适配完整性 (commit 621aa48e)

### 审计发现与修复

**后端修复：**
- PUBLIC_PATHS 补齐上游新增路径：`/api/settings/upload-limit`、`/api/frontend_plugin`
- PUBLIC_PREFIXES 补齐：`/api/frontend_plugin/`（登录页需免认证加载前端插件）
- router_extension.py 添加 `/api/auth/revoke-token` 和 `/api/auth/revoke-all-tokens` 端点
  - 上游这两个端点在 _app.py 路由移除时一起被删了，插件没有替代
- AuthMiddleware._should_skip_auth 补回 `allow_no_auth_hosts` IP 白名单检查（与上游对齐）
- 添加 `_resolve_client_ip()` 辅助函数

**前端修复：**
- requestExtension.ts：`require()` 在 ESM/Vite 不可用 → 改用 `setHandle401()`
- initializeMultiUser() 中调用 `patchRequest401Handler()` 注册 401 处理器
- authApi.ts / api/modules/auth.ts 的 updateProfile 移除 `copaw_auth_token` 残留，改用 `getApiToken()`
- HeaderUserMenu.tsx 合并完全相同的 handleSwitchUser/handleLogout

### 仍存在的低优先级问题（暂不修复）
- `muAuthApi.login` 不走 `buildAuthHeaders` — 登录时不发 auth header 是正确设计
- `HeaderUserMenu` 不响应 session 变化 — 401 会触发页面跳转，实际无影响

## 全量修复遗留问题 (commit 3ce09256)

### request.ts authHeaders 导入路径修复
- `request.ts` 从 `../multi_user/authHeaders` 改为 `./authHeaders`（分发层）
- 分发层 `api/authHeaders.ts` 根据 MULTI_USER_ENABLED 选择正确的 buildAuthHeaders
- `multi_user/authHeaders.ts` 禁用模式下也匹配上游行为：sessionStorage 优先 → localStorage fallback

### 启动窗口期 404 防护
- `_app.py` 移除上游 auth 路由后，立即注册临时 `/api/auth/status` 路由
- 返回 `{enabled: True, has_users: False, multi_user: True}`，防止前端 404
- 插件加载后会被替换为完整版本

### _PUBLIC_PATHS 命名一致
- `patch_auth_module` 同时 patch `_PUBLIC_PATHS` 和 `_PUBLIC_PREFIXES`（带下划线版本）
- 上游 auth.py 内部使用 `_PUBLIC_PATHS`，现在也会获取到多用户版的正确值

### multi_user 字段消费
- `api/modules/auth.ts` AuthStatusResponse 新增 `multi_user?` 和 `user_fields?` 可选字段
- `UpstreamAuthGuard` 检测 `multi_user=true` + `MULTI_USER_ENABLED=false` → console.warn 提示
